# One-click deploy config for Streamlit Cloud
# After pushing to GitHub: https://share.streamlit.io → New app → select this repo → main file: app.py

[deploy]
# No secrets required — users enter league ID in the app sidebar
